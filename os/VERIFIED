Gaut current verification
Date: 2026-08-15

Retained source and artifacts:
- gaut/compiler/, 6 source units, 1,954 lines, 60,051 bytes
- profile 2 compiler request, 60,211 bytes, SHA-256:
  45d6326e6a4d81c1ac58b882986d9d431875c121161a7a9b17a81ee0b4ccacb7
- os/examples/child1.gaut, 792 bytes, SHA-256:
  beae4a0687c8ab9bee67409e7f90d9784ab1b98a774ced2085a318c245e261fa
- os/examples/child2.gaut, 793 bytes, SHA-256:
  7ce5d3e709a92b413720d0070f9b00380406c7da060fa7d1aaefa6ffea0a6193
- dist/gaut.elf, 77,824-byte static AArch64 Linux bootstrap compiler
- hosted compiler SHA-256:
  6f67a428130ae2b4b4f40e4ecbeba2015b4b926f7aa94c98237be68f42c8a37d
- dist/gaut-os.img, 77,824-byte raw AArch64 resident compiler
- freestanding compiler SHA-256:
  31e16b1c847cb0dc553f2df6ddd2f047ba25f9560be7304d655566e7c305e99a

Self-host proof:
1. the retained Module v0 Gaut OS compiler built the six compiler source units
   as a new profile 2 image through its PL011 build channel
2. that first split compiler built the same six units again, and the second
   split compiler built them a third time
3. all three split generations were byte-identical at 77,824 bytes with
   SHA-256:
   31e16b1c847cb0dc553f2df6ddd2f047ba25f9560be7304d655566e7c305e99a
4. the byte-identical compiler emitted the retained profile 1 static ELF at
   77,824 bytes with SHA-256:
   6f67a428130ae2b4b4f40e4ecbeba2015b4b926f7aa94c98237be68f42c8a37d
5. this fixed-point path used QEMU and Gaut OS without a Linux guest or foreign
   compiler

Module v0 proof:
1. `app.gaut` called `math.vector.answer()` from
   `math/vector.gaut` without `module` or `import` syntax
2. the hosted result printed `module ok` and returned 42
3. the same two-unit request ran as a profile 3 child in Gaut OS, printed
   `module ok`, returned to `gaut-os: ready`, and shut down cleanly
4. an indirect two-module call cycle returned status 2, emitted zero artifact
   bytes, and wrote the canonical diagnostic
5. unaligned record boundaries were read bytewise, so the unpadded request
   worked identically under Linux and Gaut OS EL1

Resident execution proof:
1. profile 2 initialized the compiler arena at 0x47f00000 and read exact
   unpadded request records from the QEMU PL011 serial channel
2. profile 3 initialized each child arena at 0x47e00000 and returned through
   the preserved supervisor continuation
3. each child declared the maximum 917,504-byte fixed region and successfully
   wrote and read its final eight bytes at 0x47eefff8
4. the second child compiled only after the first child returned
5. the same compiler emitted ready before the first request and after every
   successful child return
6. a sixteen-byte zero record shut down the bounded session
7. the exact 119-byte serial transcript was:

gaut-os: ready
gaut-os: received 1
gaut-os: child 1
gaut-os: ready
gaut-os: received 2
gaut-os: child 2
gaut-os: ready

Low-power wait proof:
1. profile 2 enabled PL011 receive and receive-timeout wake events through the
   QEMU virt GICv2 distributor and CPU interface
2. host.read cleared stale wake state, rechecked the receive FIFO, and executed
   WFI only while it remained empty
3. after the compile-rejection ready marker, QEMU remained at 0.0 percent CPU
   for four one-second samples with no serial input
4. a valid request delivered after that delay woke the same machine, which ran
   the child and exited with status zero after the zero terminator
5. no Gaut QEMU process remained after the delayed-input run

Compile rejection recovery proof:
1. profile 2 retained its entry address in x22 before entering the compiler
2. host.exit with status zero retained the PSCI shutdown path, while a nonzero
   status discarded the failed compiler call stack and branched to that entry
3. one serial stream delivered invalid.gaut, paused after recovery, delivered
   child1.gaut, and ended with the zero terminator
4. the exact 123-byte transcript was:

gaut-os: ready
gaut-os: received 1
gaut: invalid source
gaut-os: ready
gaut-os: received 1
gaut-os: child 1
gaut-os: ready

5. QEMU reported 0.0 percent CPU during four one-second samples after the
   rejection ready marker
6. the following valid source compiled and ran in the same boot, QEMU exited
   with status zero, and no Gaut QEMU process remained

Shutdown proof:
1. the QEMU virt machine declared the HVC PSCI conduit
2. Gaut placed PSCI SYSTEM_OFF function ID 0x84000008 in x0 and executed hvc 0
3. the direct serial-file QEMU process exited with status zero
4. the retained os/run.sh path also exited with status zero without Control-C
5. no QEMU process using dist/gaut-os.img remained after either run
6. macOS reported no thermal or performance warning after verification

Machine:
- QEMU 11.1.0, virt board, Cortex-A53, 128 MiB RAM
- no network device
- compiler loaded directly with -kernel dist/gaut-os.img

Boundary:
- the guest contains no Linux, libc, dynamic loader, Python, C, Rust, LLVM,
  assembler, linker, or foreign runtime
- the host shell only frames and streams exact requests and QEMU emulates the
  machine
- compiler and child ordinary Gaut memory are separated, but there is no MMU
  or malicious-child fault containment
- the retained launcher supplies a bounded serial stream, while the resident
  host.read path can wait for delayed input without consuming a host CPU core
- complete invalid Gaut source requests recover to ready, but malformed lengths
  and child hardware faults do not yet recover
- the next verified gate is named in-memory source storage in
  docs/ROADMAP.md
