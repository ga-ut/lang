# Sum 1..10 using explicit local slots and a structured loop.
fn main
  $0 v0!       # sum
  $1 v1!       # i
  while
    v1@ $a <=
  do
    v0@ v1@ + v0!
    v1@ $1 + v1!
  endwhile
  v0@           # 55 / 0x37
endfn
