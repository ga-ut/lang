# Platform names are ordinary Gaut identifiers, not reserved syntax.

fn target(value) {
  return add(value, 1);
}

fn qemu_virt(value) {
  return target(value);
}

fn main() {
  return qemu_virt(41);
}
